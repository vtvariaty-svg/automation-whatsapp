Plano aprovado, pode implementar.

Antes de começar, faça estes ajustes no plano para deixar a implementação mais robusta e consistente com o App Router e com futura integração:

AJUSTES OBRIGATÓRIOS

1. Criar também:
apps/web/app/dashboard/fiscal/layout.tsx

Objetivo:
- centralizar o gate de acesso do módulo fiscal no layout do segmento
- bloquear todo o módulo abaixo de PRO de forma única e consistente
- reutilizar o padrão existente de planAtLeast() / useEntitlements() / UpgradeGate
- evitar duplicação do gate em todas as páginas

As páginas internas ainda podem ter checks complementares, mas o gate principal deve ficar no layout do módulo.

2. Criar componentes compartilhados do módulo em:
apps/web/components/fiscal/

Pelo menos estes componentes reutilizáveis:
- FiscalStatusBadge
- FiscalSummaryCards
- FiscalHistoryTable
- FiscalRequestTimeline
- FiscalIntegrationPanel
- FiscalEmptyState

Se fizer sentido conforme a estrutura real do projeto, pode ajustar os nomes, mas quero o módulo componentizado e não 8 páginas monolíticas.

3. Em apps/web/lib/fiscal/service.ts
não quero apenas mocks síncronos soltos.
Quero uma adapter layer preparada para troca futura por API real.

Então exponha funções assíncronas, por exemplo:
- getFiscalDashboard()
- getFiscalDocuments()
- getFiscalCustomers()
- getFiscalCatalog()
- getFiscalRequests()
- getFiscalRequestById()
- getFiscalSettings()
- simulateFiscalEmission()

Mesmo com mocks, use Promise e estrutura compatível com futura integração.

4. A modelagem do módulo deve nascer genérica, não excessivamente acoplada só a NF-e.
Além dos tipos já previstos, inclua também tipos auxiliares como:
- FiscalDocumentType
- FiscalDocumentStatus
- FiscalRequestOrigin
- FiscalPlanAccess

Objetivo:
- deixar a UI pronta para evolução futura sem retrabalho estrutural.

5. A regra de plano continua obrigatória:
- abaixo de PRO: ocultar item Fiscal no menu
- abaixo de PRO: bloquear rotas do módulo fiscal
- PRO e BUSINESS: acesso liberado

Pode seguir com implementação mantendo exatamente essa regra.

ENTREGA
Ao final quero:
- lista de arquivos criados
- lista de arquivos alterados
- resumo do que foi implementado
- como ficou o gating por plano
- quais pontos ficaram preparados para integração futura com backend fiscal real

Pode implementar agora.